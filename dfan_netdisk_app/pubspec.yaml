name: dfan_netdisk_app
description: Flutter 客户端，对接 dfan-netdisk Go 后端 /api/v1。
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  url_launcher: ^6.3.1
  mobile_scanner: ^6.0.2
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
